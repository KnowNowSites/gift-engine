---
import { guides } from "../data/guides";
---

<h1>Gift Engine</h1>

<p>Find the perfect gift ideas for every interest.</p>

<h2>Gift Guides</h2>

<ul>
  {guides.map((guide) => (
    <li>
      <a href={`/guides/${guide.slug}`}>
        {guide.title}
      </a>
    </li>
  ))}
</ul>